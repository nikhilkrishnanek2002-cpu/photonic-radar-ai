# .env.example
# Environment configuration for PHOENIX Radar AI Docker deployment
# Copy to .env and customize as needed: cp .env.example .env
# Then run: docker compose up --build

# ============================================================================
# Backend Service Configuration
# ============================================================================

# API Server Binding
API_HOST=0.0.0.0
API_PORT=5000

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Python Runtime Optimization
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# Pip behavior
PIP_NO_CACHE_DIR=1
PIP_DISABLE_PIP_VERSION_CHECK=1

# ============================================================================
# Streamlit UI Configuration
# ============================================================================

# Backend Service Connection (for UI → API communication)
# Docker Compose service name = hostname in network
API_URL=http://backend:5000

# Streamlit Server Configuration
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0
STREAMLIT_CLIENT_TOOLBAR_MODE=viewer

# Streamlit Theme (light / dark)
STREAMLIT_THEME_BASE=dark
STREAMLIT_THEME_PRIMARYCOLOR=#00D9FF

# ============================================================================
# Application Features
# ============================================================================

# Enable/Disable synthetic data fallback in dashboard
# Set to "true" when backend API is unavailable
DEMO_MODE=false

# Enable verbose logging output
DEBUG=false

# ============================================================================
# Deployment Environment
# ============================================================================

# Environment type (development / staging / production)
ENVIRONMENT=development

# Timezone (for logging timestamps)
TZ=UTC

# ============================================================================
# Performance Tuning
# ============================================================================

# Maximum number of concurrent requests
MAX_CONNECTIONS=10

# Request timeout in seconds
REQUEST_TIMEOUT=30

# Cache TTL in seconds
CACHE_TTL=2

# ============================================================================
# Security (Development Only - Never use in production!)
# ============================================================================

# Allow CORS from any origin (development only)
CORS_ALLOW_ORIGINS=*

# JWT Secret (change in production)
SECRET_KEY=dev-secret-change-in-production

# ============================================================================
# Notes & Usage
# ============================================================================

# Development Workflow:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your local settings
# 3. Run: docker compose up --build
# 4. docker-compose.override.yml will auto-load for dev settings

# Docker Compose Variable Resolution Order:
# 1. Command line arguments
# 2. .env file (created from .env.example)
# 3. docker-compose.yml defaults
# 4. docker-compose.override.yml (development)

# Accessing Services After Startup:
# - Backend API: http://localhost:8000
# - Streamlit UI: http://localhost:8501
# - Backend health: curl http://localhost:8000/health

# View Logs:
# - Backend: docker compose logs -f backend
# - Streamlit: docker compose logs -f streamlit
# - All: docker compose logs -f

# Stop Services:
# - docker compose down          # Stop and remove containers
# - docker compose down -v       # Also remove volumes

# ============================================================================
